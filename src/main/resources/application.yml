---
server:
  port: 8888
security:
  basic:
    enabled: true
  user:
    name: pluralsight
    password: pluralsight
spring:
  cloud:
    config:
      server:
        encrypt:
          enable: false
        git:
          uri: https://github.com/IgorCreator/spring-cloud-developer-experiments.git
          username: IgorCreator
          password: SecretPass
          proxy:
            https:
              host: proxy.name
              port: 'port'
          search-paths:
            - 'station*'
